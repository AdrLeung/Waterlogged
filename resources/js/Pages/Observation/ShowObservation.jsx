import AppLayout from "@/Layouts/AppLayout";

export default function ShowObservation({ observation }) {
    console.log(observation);

    return (
        <AppLayout>
            <p>this</p>
        </AppLayout>
    );
}
